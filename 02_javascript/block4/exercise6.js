function numberConverter(arr) {
	let number = 1;
	const numbers = []
	arr.forEach(element => {
		element * number;
		arr.push(numbers);
		
	});
return numbers;
}


module.exports ={
	numberConverter
}

